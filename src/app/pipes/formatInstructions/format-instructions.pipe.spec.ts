import { FormatInstructionsPipe } from './format-instructions.pipe';

describe('FormatInstructionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatInstructionsPipe();
    expect(pipe).toBeTruthy();
  });
});
